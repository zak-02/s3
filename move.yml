resources:
- name: concourse-examples
  type: git
  icon: github
  source:
    uri: https://github.com/zak-02/s3

jobs:
- name: s3-job
  plan:
  - get: concourse-examples
  - task: s3-task
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: python
      inputs:
      - name: concourse-examples
      run:
        path: sh
        args:
        - -cx
        - |
          cd concourse-examples
          pip install boto3
          python upload.py
